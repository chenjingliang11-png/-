<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>使命必达：100% 交付营销实录</title>
    <style>
        /* 核心样式：暗黑科技风 */
        :root {
            --bg-color: #0a0a0c;
            --card-bg: #16161a;
            --text-primary: #e4e4e7;
            --text-secondary: #a1a1aa;
            --accent: #3b82f6; /* 科技蓝 */
            --accent-glow: rgba(59, 130, 246, 0.5);
            --success: #10b981; /* 胜利绿 */
        }

        body {
            font-family: 'PingFang SC', 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-primary);
            margin: 0;
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* 布局装饰 - 顶部霓虹线 */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 2px;
            background: linear-gradient(90deg, var(--bg-color), var(--accent), var(--bg-color));
            z-index: 1000;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        /* --- 英雄首屏 (Hero Section) --- */
        .hero {
            text-align: center;
            padding: 100px 0;
            position: relative;
        }

        .hero::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 300px;
            height: 300px;
            background: var(--accent);
            filter: blur(150px);
            opacity: 0.2;
            z-index: -1;
        }

        .hero-tag {
            display: inline-block;
            background-color: rgba(59, 130, 246, 0.1);
            color: var(--accent);
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: bold;
            border: 1px solid rgba(59, 130, 246, 0.3);
            margin-bottom: 16px;
        }

        .hero h1 {
            font-size: 48px;
            margin: 0 0 20px 0;
            background: linear-gradient(to right, #fff, #a1a1aa);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -1px;
        }

        .hero p {
            font-size: 18px;
            color: var(--text-secondary);
            max-width: 600px;
            margin: 0 auto 40px auto;
        }

        /* --- 核心指标卡片 (Key Metrics) --- */
        .metrics-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 24px;
            margin-bottom: 60px;
        }

        .metric-card {
            background-color: var(--card-bg);
            border: 1px solid #27272a;
            border-radius: 12px;
            padding: 30px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .metric-card:hover {
            border-color: var(--accent);
            transform: translateY(-5px);
            box-shadow: 0 10px 30px -10px var(--accent-glow);
        }

        .metric-title {
            font-size: 14px;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
        }

        .metric-value {
            font-size: 40px;
            font-weight: bold;
            color: var(--accent);
            margin-bottom: 10px;
        }

        .metric-value.success {
            color: var(--success);
        }

        .metric-desc {
            font-size: 14px;
            color: var(--text-secondary);
        }

        /* 装饰用的小图标 placeholder */
        .card-icon {
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 24px;
            opacity: 0.2;
        }

        /* 响应式调整 */
        @media (max-width: 768px) {
            .hero h1 { font-size: 32px; }
            .metrics-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>

    <div class="container">
        <header class="hero">
            <div class="hero-tag">项目管理案例：金融整合营销</div>
            <h1>使命必达：100% 交付</h1>
            <p>通过工程级的严谨逻辑，驱动 11 人团队实现 2026 年度“开门红”销售目标的精准达成。</p>
        </header>

        <main>
            <div class="metrics-grid">
                <div class="metric-card">
                    <div class="card-icon">🎯</div>
                    <div class="metric-title">项目目标达成率</div>
                    <div class="metric-value success">100%</div>
                    <div class="metric-desc">不仅是业绩达成，更是全面、高质量的营销交付。</div>
                </div>

                <div class="metric-card">
                    <div class="card-icon">💰</div>
                    <div class="metric-title">项目总产值 (APE)</div>
                    <div class="metric-value">¥396,486</div>
                    <div class="metric-desc">在限定营销周期内实现的高价值业务增长。</div>
                </div>

                <div class="metric-card">
                    <div class="card-icon">👥</div>
                    <div class="metric-title">团队协同规模</div>
                    <div class="metric-value">11 人</div>
                    <div class="metric-desc">负责任务分配、进度把控、激励与风险管理。</div>
                </div>
            </div>
        </main>
    </div>

</body>
</html>